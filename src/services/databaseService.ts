/**
 * Database service
 * Calls Tauri backend commands for gamepad and app data
 */
export const databaseService = {
  // Database services placeholder for future use
  // Currently used by gamepad profile management via Tauri commands
};
